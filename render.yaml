services:
  - type: web
    name: drive-ai-backend
    rootDirectory: drive-ai-agent
    env: python
    buildCommand: pip install -r backend/requirements.txt
    startCommand: ./start.sh
    envVars:
      - key: PYTHONPATH
        value: /opt/render/project/src/drive-ai-agent
