services:
  - type: web
    name: drive-ai-backend
    rootDirectory: drive-ai-agent
    runtime: python
    buildCommand: pip install -r backend/requirements.txt
    startCommand: uvicorn backend.app.main:app --host 0.0.0.0 --port $PORT
